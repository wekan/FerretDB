# Build context for Dockerfile.release: only the downloaded release binaries
# (dist/ferretdb-<arch>) and the passwd/group files for the scratch image.
# Everything else (source tree, .git) is excluded. This is a per-Dockerfile ignore
# file so it does not affect the source build's .dockerignore.
**
!dist
!build/ferretdb
